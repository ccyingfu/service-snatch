# Network Request Monitor - 详细开发计划

> 基于 plan1.md 的细化实现计划，从零开始构建浏览器网络请求监控插件

---

## 一、需求细化

### 1.1 核心用户场景

```
作为前端开发者，我需要：
├─ 场景1：录制线上/测试环境的 API 请求响应数据
├─ 场景2：将录制数据转换为本地 Mock 数据
├─ 场景3：按服务/接口分类管理录制的数据
└─ 场景4：一键导出 JSON 格式的 Mock 数据文件
```

### 1.2 功能需求矩阵

| 功能模块 | 优先级 | MVP | V1.0 | V2.0 |
|---------|-------|-----|------|------|
| 录制开关控制 | P0 | ✅ | ✅ | ✅ |
| XHR/Fetch 请求捕获 | P0 | ✅ | ✅ | ✅ |
| 响应体捕获 | P0 | ✅ | ✅ | ✅ |
| 服务名称自动识别 | P0 | ✅ | ✅ | ✅ |
| 请求列表展示 | P0 | ✅ | ✅ | ✅ |
| JSON 导出 | P0 | ✅ | ✅ | ✅ |
| 静态资源过滤 | P1 | ✅ | ✅ | ✅ |
| URL 过滤规则 | P1 | - | ✅ | ✅ |
| 请求详情查看 | P1 | - | ✅ | ✅ |
| 数据脱敏 | P1 | - | ✅ | ✅ |
| 多格式导出 (MSW/Mock.js) | P2 | - | - | ✅ |
| 请求编辑重放 | P2 | - | - | ✅ |
| 会话管理 | P2 | - | - | ✅ |

### 1.3 数据结构设计

#### 录制数据结构
```typescript
interface RecordedData {
  meta: {
    pageUrl: string;           // 页面 URL
    pageTitle: string;         // 页面标题
    startTime: number;         // 录制开始时间
    endTime?: number;          // 录制结束时间
    totalRequests: number;     // 请求总数
  };
  services: {
    [serviceName: string]: {
      name: string;            // 服务名称
      baseUrl: string;         // 基础 URL
      requests: RequestRecord[];
    };
  };
}

interface RequestRecord {
  id: string;                  // 唯一标识
  timestamp: number;           // 时间戳
  method: string;              // HTTP 方法
  url: string;                 // 完整 URL
  path: string;                // 路径部分
  request: {
    headers: Record<string, string>;
    query: Record<string, string>;
    body?: any;
  };
  response: {
    status: number;
    statusText: string;
    headers: Record<string, string>;
    body: any;
    size: number;              // 响应大小
    time: number;              // 响应时间 (ms)
  };
}
```

#### 导出 Mock 数据结构
```json
{
  "version": "1.0",
  "exportTime": "2026-02-28T10:30:00Z",
  "source": "https://example.com/dashboard",
  "mocks": [
    {
      "service": "user-service",
      "endpoint": "/api/user/profile",
      "method": "GET",
      "response": {
        "status": 200,
        "data": { "id": 1, "name": "John" }
      }
    }
  ]
}
```

---

## 二、技术方案

### 2.1 架构设计

```
┌─────────────────────────────────────────────────────────┐
│                     Chrome Extension                     │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │
│  │   Popup     │  │   Content   │  │    Background   │ │
│  │    UI       │  │   Script    │  │  Service Worker │ │
│  │             │  │             │  │                 │ │
│  │ • 录制控制  │  │ • 脚本注入  │  │ • Debugger API  │ │
│  │ • 请求列表  │  │ • 消息桥接  │  │ • 数据处理      │ │
│  │ • 导出功能  │  │             │  │ • 存储管理      │ │
│  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘ │
│         │                │                   │          │
│         └────────────────┴───────────────────┘          │
│                    Chrome Message API                    │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心技术选型

| 技术点 | 方案 | 原因 |
|-------|------|------|
| 请求拦截 | Chrome Debugger API | 可获取完整响应体，比 webRequest 功能更全 |
| 数据存储 | chrome.storage.local | MVP 阶段足够，后续可升级 IndexedDB |
| UI 框架 | 原生 HTML/CSS/JS | 轻量，无构建依赖 |
| 构建工具 | 无 (MVP) / Vite (V2.0) | 渐进式复杂度 |

### 2.3 请求捕获方案

**方案：Chrome Debugger API**

```javascript
// 附加调试器
chrome.debugger.attach({ tabId }, "1.3");

// 启用网络监控
chrome.debugger.sendCommand({ tabId }, "Network.enable");

// 监听响应
chrome.debugger.onEvent.addListener((source, method, params) => {
  if (method === "Network.responseReceived") {
    // 获取响应体
    chrome.debugger.sendCommand(
      { tabId: source.tabId },
      "Network.getResponseBody",
      { requestId: params.requestId }
    );
  }
});
```

**优势**：
- ✅ 可获取完整响应体
- ✅ 支持所有请求类型
- ✅ 不需要注入页面脚本

**限制**：
- ⚠️ 会显示"正在调试此浏览器"提示
- ⚠️ 需要 debugger 权限

---

## 三、项目结构

```
browser-plugin/
├── manifest.json              # 插件配置 (Manifest V3)
├── background/
│   ├── service-worker.js      # 后台服务主入口
│   ├── network-monitor.js     # 网络监控核心逻辑
│   ├── data-processor.js      # 数据处理与服务分类
│   └── storage-manager.js     # 存储管理
├── popup/
│   ├── popup.html             # 弹窗页面
│   ├── popup.css              # 样式
│   ├── popup.js               # 主逻辑
│   └── components/
│       ├── request-list.js    # 请求列表组件
│       ├── service-group.js   # 服务分组组件
│       └── export-panel.js    # 导出面板组件
├── utils/
│   ├── url-parser.js          # URL 解析工具
│   ├── service-detector.js    # 服务名称识别
│   ├── filter.js              # 过滤工具
│   └── exporter.js            # 导出工具
├── assets/
│   ├── icons/
│   │   ├── icon-16.png
│   │   ├── icon-48.png
│   │   └── icon-128.png
│   └── styles/
│       └── common.css
├── plan1.md
└── plan2.md
```

---

## 四、开发任务拆分

### Phase 1: 项目初始化 (0.5 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T1.1 | 创建 manifest.json | 插件基础配置 | 15min |
| T1.2 | 创建目录结构 | 项目骨架 | 10min |
| T1.3 | 设计图标资源 | 3 个尺寸图标 | 30min |
| T1.4 | 创建 Popup 基础页面 | HTML/CSS 框架 | 1h |
| T1.5 | 实现 Background 基础框架 | Service Worker 入口 | 30min |

**验收标准**: 插件可加载到 Chrome，点击图标显示空白 Popup

---

### Phase 2: 网络请求捕获 (1.5 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T2.1 | 实现 Debugger API 连接 | 调试器附加/分离 | 2h |
| T2.2 | 实现请求监听 | 捕获 requestWillBeSent | 2h |
| T2.3 | 实现响应监听 | 捕获 responseReceived | 2h |
| T2.4 | 实现响应体获取 | getResponseBody | 2h |
| T2.5 | 实现静态资源过滤 | 排除 js/css/img 等 | 1h |
| T2.6 | 实现标签页状态管理 | 多标签页支持 | 1h |

**验收标准**: 控制台可打印捕获的请求/响应数据

---

### Phase 3: 数据处理与存储 (1 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T3.1 | 实现 URL 解析器 | 提取 host/path/query | 1h |
| T3.2 | 实现服务名称识别 | 从 URL 提取服务名 | 2h |
| T3.3 | 实现数据结构化 | 按服务分组存储 | 2h |
| T3.4 | 实现 Storage 存储 | chrome.storage.local | 1.5h |
| T3.5 | 实现数据清理 | 清空/删除功能 | 0.5h |

**服务名称识别规则**:
```javascript
// 优先级从高到低
1. 子域名识别: api.user.example.com → "user"
2. 路径前缀识别: /api/v1/user/profile → "user"
3. 域名识别: user-service.example.com → "user-service"
4. 默认: 使用完整 host
```

**验收标准**: 请求按服务名称分组存储

---

### Phase 4: Popup UI 开发 (1.5 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T4.1 | 实现录制控制 UI | 开始/停止按钮 | 1h |
| T4.2 | 实现状态显示 | 录制状态/请求计数 | 1h |
| T4.3 | 实现服务列表 | 可折叠的服务分组 | 2h |
| T4.4 | 实现请求列表 | 方法/URL/状态码展示 | 2h |
| T4.5 | 实现请求详情 | 点击查看完整数据 | 2h |
| T4.6 | 样式美化 | 专业的视觉设计 | 2h |

**UI 设计稿**:
```
┌─────────────────────────────────┐
│  🔴 Network Monitor      [⚙️]  │
├─────────────────────────────────┤
│  ┌─────────────────────────┐   │
│  │  [▶️ 开始录制] [🗑️清空] │   │
│  └─────────────────────────┘   │
│                                 │
│  📊 已捕获: 23 个请求          │
│                                 │
│  ▼ user-service (8)            │
│    GET /api/user/profile  200  │
│    POST /api/user/login   200  │
│    ...                         │
│  ▶ order-service (15)          │
│                                 │
├─────────────────────────────────┤
│  [📥 导出 JSON]                │
└─────────────────────────────────┘
```

**验收标准**: 完整的 UI 交互，可查看录制数据

---

### Phase 5: 导出功能 (0.5 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T5.1 | 实现数据格式化 | 转换为导出格式 | 1h |
| T5.2 | 实现文件下载 | JSON 文件生成 | 1h |
| T5.3 | 实现导出选项 | 选择服务/全部导出 | 1h |
| T5.4 | 实现文件命名 | 时间戳/页面名称 | 0.5h |

**验收标准**: 可导出格式正确的 JSON Mock 数据文件

---

### Phase 6: 测试与优化 (1 天)

| Task ID | 任务 | 产出 | 预估 |
|---------|------|------|------|
| T6.1 | 功能测试 | 测试用例执行 | 2h |
| T6.2 | 兼容性测试 | 不同网站测试 | 2h |
| T6.3 | 性能优化 | 大量请求处理优化 | 2h |
| T6.4 | Bug 修复 | 问题修复 | 2h |

**验收标准**: 插件稳定可用

---

## 五、开发时间线

```
Week 1
├── Day 1: Phase 1 (项目初始化) ✓
├── Day 2-3: Phase 2 (网络请求捕获)
├── Day 4: Phase 3 (数据处理与存储)
├── Day 5-6: Phase 4 (Popup UI 开发)
└── Day 7: Phase 5 + Phase 6 (导出功能 + 测试)

总计: 6-7 工作日
```

---

## 六、关键代码示例

### 6.1 manifest.json
```json
{
  "manifest_version": 3,
  "name": "Network Request Monitor",
  "version": "1.0.0",
  "description": "监控浏览器网络请求，生成 Mock 测试数据",
  "permissions": [
    "debugger",
    "storage",
    "activeTab",
    "tabs"
  ],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background/service-worker.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup/popup.html",
    "default_icon": {
      "16": "assets/icons/icon-16.png",
      "48": "assets/icons/icon-48.png",
      "128": "assets/icons/icon-128.png"
    }
  },
  "icons": {
    "16": "assets/icons/icon-16.png",
    "48": "assets/icons/icon-48.png",
    "128": "assets/icons/icon-128.png"
  }
}
```

### 6.2 核心监控逻辑 (伪代码)
```javascript
// background/network-monitor.js
class NetworkMonitor {
  constructor(tabId) {
    this.tabId = tabId;
    this.requests = new Map();
    this.isRecording = false;
  }

  async start() {
    await chrome.debugger.attach({ tabId: this.tabId }, "1.3");
    await chrome.debugger.sendCommand({ tabId: this.tabId }, "Network.enable");
    this.isRecording = true;
  }

  async stop() {
    await chrome.debugger.detach({ tabId: this.tabId });
    this.isRecording = false;
  }

  handleDebuggerEvent(method, params) {
    switch (method) {
      case "Network.requestWillBeSent":
        this.onRequestStart(params);
        break;
      case "Network.responseReceived":
        this.onResponseReceived(params);
        break;
    }
  }

  async onResponseReceived(params) {
    const { body } = await chrome.debugger.sendCommand(
      { tabId: this.tabId },
      "Network.getResponseBody",
      { requestId: params.requestId }
    );
    // 存储响应数据...
  }
}
```

---

## 七、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| Debugger API 权限警告 | 用户体验 | 在说明中告知用户，属正常现象 |
| 大量请求导致内存问题 | 性能 | 设置请求数量上限，超出自动清理旧数据 |
| 响应体获取失败 | 功能 | 添加错误处理，记录失败原因 |
| 跨域请求问题 | 功能 | host_permissions 设置 `<all_urls>` |

---

## 八、后续迭代方向

### V1.1 - 体验优化
- [ ] 添加 URL 白名单/黑名单过滤
- [ ] 敏感数据脱敏功能
- [ ] 请求详情复制功能

### V2.0 - 功能扩展
- [ ] MSW / Mock.js 格式导出
- [ ] 请求编辑与重放
- [ ] 会话持久化存储
- [ ] 深色模式支持

---

## 九、验收清单

### MVP 验收 ✅
- [ ] 插件可正常加载到 Chrome
- [ ] 点击"开始录制"可捕获网络请求
- [ ] 请求按服务名称分组显示
- [ ] 可查看请求的完整详情
- [ ] 可导出 JSON 格式 Mock 数据
- [ ] 导出数据格式正确可用

---

---

**文档版本:** v2.0
**创建时间:** 2026-02-28
**基于:** plan1.md
